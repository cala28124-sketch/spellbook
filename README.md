A currently WIP design for a digital DND themed spellbook, allowing for insertion and cataloging of spells.

To run, simply do either pnpm backend/pnpm frontend in root folder. To run tauri, run the command pnpm tauri dev instead of pnpm frontend to launch the tauri application.
